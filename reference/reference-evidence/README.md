# Reference evidence for `mobile-slice-adaptation-20260830`

This directory is isolated to one game run. It is not a general media inbox.

## Intake rules

1. Put newly supplied files in `incoming/` only after recording the exact source path.
2. Add every file to `manifest.json` with `targetGame`, `workspace`, `sha256`, `sourcePath`, `identityStatus`, and `reviewStatus`.
3. A file may move to `verified/` only after its game identity is confirmed against the target run and its hash is recorded.
4. Unknown or mismatched media stays in `excluded/` as a provenance record and must never be used as gameplay evidence.
5. QA captures generated by this workspace stay under the run's `qa*/` directories; they are not reference recordings.

The three user-mentioned DingTalk recordings (`140550`, `140908`, `141313`) are currently unavailable at their supplied paths. Do not substitute another recording with a similar timestamp.
