import { z } from 'zod';

export const CYCLE_ID = 'human-review-20260923-continue';
export const TARGET_GAME = 'mobile-slice-adaptation-20260830';
export const WORKSPACE = '/Users/kker/Documents/ChatGPT/妖怪夜市/runs/mobile-slice-adaptation-20260830/workspace/prototype-a';

const sha256 = z.string().regex(/^[a-f0-9]{64}$/);
const path = z.string().min(1);
const viewport = z.enum(['390x844', '1100x720', 'deterministic-fixed-step']);

const EvidenceRefSchema = z.object({
  path,
  sha256,
  role: z.enum(['natural-reproduction', 'prior-fix-receipt', 'prior-experiment-rejection', 'prior-cycle-state', 'current-build', 'current-source']),
}).strict();

const MatrixRowSchema = z.object({
  objectType: z.string().min(1),
  lifecycleStateBranch: z.string().min(1),
  rendererPresentationPath: z.string().min(1),
  viewport,
  evidence: z.array(EvidenceRefSchema).min(1),
  observed: z.string().min(1),
  inference: z.string().min(1),
  unknowns: z.array(z.string()).min(1),
}).strict();

export const TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  issueId: z.literal('L1-RUNWAY-EARLY-SHARP-ANCHOR'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  reproduction: z.object({
    freshContext: z.literal(true),
    inputMode: z.literal('visible normal touch/mouse; no fixture, debug API, or state injection'),
    observations: z.array(z.object({
      viewport,
      supportId: z.string().min(1),
      playerX: z.number(),
      elapsedSeconds: z.number().nonnegative(),
      cuts: z.number().int().nonnegative(),
      terminal: z.null(),
      replay: z.null(),
      evidence: EvidenceRefSchema,
    }).strict()).length(2),
    result: z.string().min(1),
  }).strict(),
  reproductionMatrix: z.array(MatrixRowSchema).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    expectedContact: z.object({
      supportId: z.literal('level1-runway-main'),
      part: z.enum(['tip', 'edge']),
      normalYAtOrBelow: z.number().max(-0.9),
      approachOnly: z.literal(true),
      earlyXRange: z.tuple([z.number(), z.number()]),
      contactPointX: z.number(),
    }).strict(),
    rejectedAlternatives: z.array(z.string()).min(1),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(8),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  priorCycle: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    historyPreserved: z.literal(true),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const AuthorizationSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('human-repair-authorization'),
  cycleId: z.literal(CYCLE_ID),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  userReply: z.literal('继续吧'),
  authorizedAt: z.string().datetime(),
  maximumAttempts: z.null(),
  repairCeiling: z.literal('none-configured'),
  triagePath: path,
  triageSha256: sha256,
  previousCyclePath: path,
  previousCycleSha256: sha256,
  storedThreadId: z.literal('01a07540-7ee1-7c01-899b-0ea12bde1f19'),
  historyPreserved: z.literal(true),
  modelPolicy: z.literal('gpt-6-luna'),
}).strict();

const AttemptSummarySchema = z.object({
  attempt: z.number().int().positive(),
  taskPath: path,
  status: z.enum(['READY_FOR_FIX', 'FIX_IN_PROGRESS', 'QA_PENDING', 'BLOCKED', 'PASSED']),
  reservedAt: z.string().datetime(),
  fixReceiptPath: path.optional(),
  completedAt: z.string().datetime().optional(),
}).strict();

export const CycleStateSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-cycle-state'),
  cycleId: z.literal(CYCLE_ID),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  maximumAttempts: z.null(),
  attemptsStarted: z.number().int().nonnegative(),
  attemptsCompleted: z.number().int().nonnegative(),
  currentAttempt: z.number().int().positive(),
  status: z.enum(['READY_FOR_FIX', 'FIX_IN_PROGRESS', 'QA_PENDING', 'BLOCKED', 'PASSED']),
  authorizationPath: path,
  storedThreadId: z.literal('01a07540-7ee1-7c01-899b-0ea12bde1f19'),
  historyPreserved: z.literal(true),
  attempts: z.array(AttemptSummarySchema),
}).strict();

export const TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(1),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-EARLY-SHARP-ANCHOR'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(8),
  evidenceInputs: z.array(EvidenceRefSchema).min(5),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const QaTaskSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('independent-qa-task'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(1),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('QAAgent'),
  model: z.literal('gpt-6-luna'),
  fixReceiptPath: path,
  fixReceiptSha256: sha256,
  expectedBuildSha256: z.literal('599b4c3cb8c6b345b7e18b3a4e06cae33663c0432b918d3115ee78df3bf64118'),
  viewports: z.array(z.object({
    label: z.enum(['portrait-390x844', 'desktop-1100x720']),
    width: z.number().int().positive(),
    height: z.number().int().positive(),
    input: z.enum(['touch', 'mouse']),
    outputDirectory: path,
  }).strict()).length(2),
  naturalInputRules: z.object({
    freshContextPerViewport: z.literal(true),
    stateInjectionForbidden: z.literal(true),
    debugApiForbidden: z.literal(true),
    adaptiveStateSchedulingForbidden: z.literal(true),
    visibleInputOnly: z.literal(true),
  }).strict(),
  requiredEvidence: z.array(z.string().min(1)).min(8),
  exactBranchChecks: z.array(z.string().min(1)).min(6),
  terminalChecks: z.array(z.string().min(1)).min(4),
  perceptualChecks: z.array(z.string().min(1)).min(4),
  statusPolicy: z.enum(['PASS_ONLY_IF_ALL_VIEWPORTS_AND_GATES_PASS', 'BLOCKED_ON_ANY_VIEWPORT_GAP']),
  createdAt: z.string().datetime(),
}).strict();

export const QaSummarySchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('independent-qa-result'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(1),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  status: z.literal('BLOCKED'),
  fixReceiptSha256: sha256,
  expectedBuildSha256: z.literal('599b4c3cb8c6b345b7e18b3a4e06cae33663c0432b918d3115ee78df3bf64118'),
  sourceBuildLock: z.object({
    portraitUnchanged: z.literal(true),
    desktopUnchanged: z.literal(true),
    buildUnchanged: z.literal(true),
  }).strict(),
  runs: z.array(z.object({
    viewport: z.enum(['portrait-390x844', 'desktop-1100x720']),
    schedule: z.enum(['550ms', '950ms', 'static-spaced', 'desktop-white-opening']),
    recordingPath: path,
    recordingSha256: sha256,
    terminal: z.object({
      outcome: z.enum(['failed', 'won']),
      exactCopy: z.string().min(1),
      stateStatus: z.enum(['failed', 'won']),
      visiblePixels: z.literal(true),
    }).strict(),
    replay: z.object({
      clickedVisibleControl: z.literal(true),
      returnedReady: z.literal(true),
    }).strict(),
    maxPlayerX: z.number(),
    consoleErrorCount: z.number().int().nonnegative(),
  }).strict()).min(4),
  exactBranch: z.object({
    portrait: z.object({
      status: z.literal('BLOCKED'),
      observedRunwaySharpAnchors: z.array(z.object({
        playerX: z.number(),
        contactX: z.number(),
        normalY: z.literal(-1),
        contactPart: z.literal('tip'),
      }).strict()).min(1),
      laterMaxPlayerX: z.number(),
      stackObserved: z.literal(false),
    }).strict(),
    desktop: z.object({
      status: z.literal('BLOCKED'),
      observedRunwaySharpAnchors: z.array(z.object({
        playerX: z.number(),
        contactX: z.number(),
        normalY: z.literal(-1),
        contactPart: z.literal('tip'),
      }).strict()).min(1),
      laterMaxPlayerX: z.number(),
      stackObserved: z.literal(true),
      stackEvidence: z.array(z.string().min(1)).min(1),
    }).strict(),
  }).strict(),
  perceptualGate: z.object({
    status: z.literal('BLOCKED'),
    inspectedFrames: z.array(path).min(4),
    blockers: z.array(z.string().min(1)).min(2),
  }).strict(),
  blockers: z.array(z.string().min(1)).min(4),
  evidence: z.array(EvidenceRefSchema).min(8),
  completedAt: z.string().datetime(),
}).strict();

export const Attempt2TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(2),
  issueId: z.literal('L1-RUNWAY-TOP-CONTACT-DIRECTION'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorQa: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
  }).strict(),
  reproduction: z.object({
    viewport: z.enum(['390x844', '1100x720']),
    schedule: z.enum(['static-spaced', '950ms']),
    supportId: z.literal('level1-runway-main'),
    contactPart: z.literal('tip'),
    normalY: z.literal(-1),
    observedPlayerX: z.array(z.number()).min(2),
    observedContactX: z.array(z.number()).min(2),
    statusAtContact: z.literal('anchored'),
    evidencePath: path,
    evidenceSha256: sha256,
    naturalInputOnly: z.literal(true),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    relativeNormalVelocitySign: z.literal('positive-or-zero'),
    proposedGuard: z.literal('level1-runway-main && sharpHit.contact.normalY <= -0.9'),
    preservedResponse: z.literal('respondToHardSurface'),
    rejectedAlternatives: z.array(z.string().min(1)).min(1),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(8),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt2TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(2),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-TOP-CONTACT-DIRECTION'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(8),
  evidenceInputs: z.array(EvidenceRefSchema).min(6),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt3TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(3),
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-LIFECYCLE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    objectType: z.literal('level1-runway-main sharp tip/normalY=-1'),
    naturalViewport: z.enum(['390x844', '1100x720']),
    naturalContactPlayerX: z.array(z.number()).min(2),
    naturalContactX: z.array(z.number()).min(2),
    naturalContactVy: z.array(z.number()).min(1),
    baselineReplayAnchorTimes: z.array(z.number()).min(2),
    baselineReplayReleaseTapTimes: z.array(z.number()).min(2),
    candidateReplayFailure: z.object({
      elapsedSeconds: z.number(),
      playerX: z.number(),
      failReason: z.string().min(1),
    }).strict(),
    evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    latchName: z.literal('pendingRunwayReleaseIntent'),
    latchSemantics: z.literal('next normal flip tap consumes the pending early runway recovery using anchored-release launch semantics'),
    expirySeconds: z.number().positive(),
    clearOn: z.array(z.enum(['reset', 'loadLevel', 'terminal', 'non-runway-contact', 'expiry'])).min(4),
    rejectedAlternatives: z.array(z.string().min(1)).min(2),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(9),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt3TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(3),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-LIFECYCLE'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(9),
  evidenceInputs: z.array(EvidenceRefSchema).min(7),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt4TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(4),
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-VELOCITY'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    objectType: z.literal('level1-runway-main sharp tip/normalY=-1'),
    naturalViewport: z.enum(['390x844', '1100x720']),
    naturalContactPlayerX: z.array(z.number()).min(2),
    naturalContactX: z.array(z.number()).min(2),
    naturalContactVy: z.array(z.number()).min(1),
    baselineReplayAnchorTimes: z.array(z.number()).min(2),
    baselineReplayReleaseTapTimes: z.array(z.number()).min(2),
    candidateReplayFailure: z.object({
      elapsedSeconds: z.number(),
      playerX: z.number(),
      playerY: z.number(),
      failReason: z.string().min(1),
    }).strict(),
    evidence: z.array(z.object({ path, sha256 }).strict()).min(4),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    latchName: z.literal('pendingRunwayReleaseIntent'),
    velocitySnapshotName: z.literal('pendingRunwayReleaseVy'),
    snapshotSemantics: z.literal('capture the post-recovery outgoing vertical velocity and use it once instead of overwriting it with LAUNCH_VY'),
    expirySeconds: z.number().positive(),
    rejectedAlternatives: z.array(z.string().min(1)).min(3),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(9),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt4TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(4),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-VELOCITY'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(9),
  evidenceInputs: z.array(EvidenceRefSchema).min(8),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt5TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(5),
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-TAP-TIME'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    naturalContactPlayerX: z.array(z.number()).min(2),
    naturalContactX: z.array(z.number()).min(2),
    firstReleaseTapSeconds: z.literal(2.327048167),
    candidateFailureSeconds: z.literal(6.8),
    candidateFailureEvidence: z.object({ path, sha256 }).strict(),
    evidence: z.array(z.object({ path, sha256 }).strict()).min(4),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    latchName: z.literal('pendingRunwayReleaseIntent'),
    tapTimeVelocityName: z.literal('pendingRunwayReleaseVyAtTap'),
    tapTimeSemantics: z.literal('derive the one-shot release vertical velocity from the current player velocity at act flip time, with no stale contact snapshot'),
    expirySeconds: z.number().positive(),
    rejectedAlternatives: z.array(z.string().min(1)).min(3),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(9),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt5TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(5),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-TAP-TIME'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(9),
  evidenceInputs: z.array(EvidenceRefSchema).min(8),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt6TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(6),
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-CLEARANCE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    naturalContactPlayerX: z.array(z.number()).min(2),
    naturalContactX: z.array(z.number()).min(2),
    releaseTapSeconds: z.literal(2.327048167),
    candidateReplayState: z.object({
      elapsedSeconds: z.literal(11.308333333333332),
      playerX: z.literal(1802.375088932486),
      playerY: z.literal(450.13086605186174),
      status: z.literal('anchored'),
      anchorId: z.literal('lift-plank'),
    }).strict(),
    evidence: z.array(z.object({ path, sha256 }).strict()).min(4),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    latchName: z.literal('pendingRunwayReleaseIntent'),
    clearanceSource: z.literal('next authored active support above or at the current forward route'),
    releaseSemantics: z.literal('derive bounded vertical release velocity from current tap-time vy and the next-support vertical clearance, without unconditional LAUNCH_VY'),
    expirySeconds: z.number().positive(),
    rejectedAlternatives: z.array(z.string().min(1)).min(4),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(10),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt6TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(6),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-RELEASE-CLEARANCE'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(10),
  evidenceInputs: z.array(EvidenceRefSchema).min(8),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt7TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(7),
  issueId: z.literal('L1-RUNWAY-RECOVERY-SUPPORT-TRACE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    naturalContactPlayerX: z.array(z.number()).min(2),
    naturalContactX: z.array(z.number()).min(2),
    candidateReplaySeconds: z.literal(5.466666666666728),
    candidateFailureEvidence: z.object({ path, sha256 }).strict(),
    priorLateSupportEvidence: z.object({ path, sha256 }).strict(),
    evidence: z.array(z.object({ path, sha256 }).strict()).min(5),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    traceName: z.literal('pendingRunwayReleaseSupportTrace'),
    traceFields: z.array(z.enum(['selectedSupportId', 'entryTime', 'supportTop', 'verticalBand', 'predictedY', 'requiredVy', 'upperBoundaryLimit'])).min(7),
    selectionRule: z.literal('select only an active collidable authored support ahead of the player whose predicted vertical corridor is reachable; preserve lift-plank as an explicit late-route assertion'),
    rejectedAlternatives: z.array(z.string().min(1)).min(4),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(10),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt7TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(7),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-SUPPORT-TRACE'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(10),
  evidenceInputs: z.array(EvidenceRefSchema).min(9),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt8TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(8),
  issueId: z.literal('L1-RUNWAY-RECOVERY-VIRTUAL-POSE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    naturalContactPlayerX: z.array(z.number()).min(2),
    naturalContactX: z.array(z.number()).min(2),
    baselineRunwayAnchorPose: z.array(z.object({
      supportId: z.literal('level1-runway-main'),
      playerX: z.number(),
      playerY: z.number(),
      playerAngle: z.number(),
    }).strict()).length(2),
    baselineReleaseTapSeconds: z.array(z.number()).length(2),
    priorFailureEvidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    evidence: z.array(z.object({ path, sha256 }).strict()).min(5),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    intentName: z.literal('pendingRunwayReleaseIntent'),
    poseFields: z.array(z.enum(['supportId', 'anchorOffsetX', 'anchorOffsetY', 'playerX', 'playerY', 'playerAngle'])).length(6),
    lifecycleSemantics: z.literal('remain visibly airborne while retaining a virtual support-release pose; consume exactly once by launching from the stored pose and current support transform'),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(11),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt8TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(8),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-VIRTUAL-POSE'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(11),
  evidenceInputs: z.array(EvidenceRefSchema).min(10),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt9TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(9),
  issueId: z.literal('L1-RUNWAY-RECOVERY-VIRTUAL-POSE-TTL'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    baselineTimeline: z.array(z.object({
      ordinal: z.enum(['first-runway-contact', 'first-release-tap', 'second-runway-contact', 'second-release-tap']),
      frame: z.number().int().nonnegative(),
      wallTimeSeconds: z.number().nonnegative(),
      simulationElapsedSeconds: z.number().nonnegative(),
      playerX: z.number(),
      playerY: z.number(),
      status: z.enum(['airborne', 'anchored', 'failed', 'won', 'ready']),
      anchorId: z.string().nullable(),
    }).strict()).length(4),
    baselineRunwayReleasePoses: z.array(z.object({
      playerX: z.number(),
      playerY: z.number(),
      playerAngle: z.number(),
      tapSeconds: z.number(),
    }).strict()).length(2),
    candidateObservation: z.object({
      secondTapSeconds: z.number(),
      pendingPosePresent: z.literal(false),
      replayElapsedSeconds: z.number(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(2),
    }).strict(),
    priorFailureEvidence: z.array(z.object({ path, sha256 }).strict()).min(3),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    intentName: z.literal('pendingRunwayReleaseIntent'),
    requiredSecondPoseLifetimeSeconds: z.number().min(2.09),
    traceRequirement: z.literal('record every set, consume, expiry, reset, terminal, non-runway, and inactive-support clear with age and reason before changing TTL'),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(12),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt9TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(9),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-VIRTUAL-POSE-TTL'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(12),
  evidenceInputs: z.array(EvidenceRefSchema).min(10),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt10TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(10),
  issueId: z.literal('L1-RUNWAY-RECOVERY-SECOND-SETTER-GATE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    baselineContacts: z.array(z.object({
      ordinal: z.enum(['first', 'second']),
      frame: z.number().int().positive(),
      worldTimeSeconds: z.number().nonnegative(),
      preContactPlayerX: z.number(),
      preContactPlayerY: z.number(),
      preContactVy: z.number(),
      contactPointX: z.number(),
      contactPointY: z.number(),
      normalX: z.number(),
      normalY: z.number(),
      relativeNormalVelocity: z.number(),
      anchorPlayerX: z.number(),
      anchorPlayerY: z.number(),
      anchorAngle: z.number(),
    }).strict()).length(2),
    authoredSuppressionBoundaryX: z.number(),
    candidateObservation: z.object({
      firstPoseMatched: z.literal(true),
      secondPosePresentAtTap: z.literal(false),
      secondTapSeconds: z.number(),
      replayElapsedSeconds: z.number(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(2),
    }).strict(),
    evidence: z.array(z.object({ path, sha256 }).strict()).min(5),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    setterName: z.literal('pendingRunwayReleaseIntent'),
    gateRequirement: z.literal('accept both early separating runway top-tip contacts at pre-contact player x 303.95 and 331.45 while preserving the authored x>=350 suppression boundary and side/body branches'),
    traceRequirement: z.literal('record pre-contact x, normalY, relativeNormalVelocity, branch decision, pose set/skip reason, and pending age for both contacts before changing any TTL'),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(12),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt10TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(10),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-SECOND-SETTER-GATE'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(12),
  evidenceInputs: z.array(EvidenceRefSchema).min(10),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt11TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(11),
  issueId: z.literal('L1-RUNWAY-RECOVERY-VIRTUAL-POSE-RECONSTRUCTION'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    baselineRunwayContacts: z.array(z.object({
      frame: z.number().int().positive(),
      playerX: z.number(),
      playerY: z.number(),
      playerAngle: z.number(),
      contactX: z.number(),
      contactY: z.number(),
      normalY: z.number(),
      relativeNormalVelocity: z.number(),
    }).strict()).length(2),
    baselineReleasePoses: z.array(z.object({
      tapSeconds: z.number(),
      playerX: z.number(),
      playerY: z.number(),
      playerAngle: z.number(),
    }).strict()).length(2),
    secondPoseHoldSeconds: z.number().min(2),
    naturalEvidence: z.array(z.object({ path, sha256 }).strict()).min(2),
    priorFailureEvidence: z.array(z.object({ path, sha256 }).strict()).min(4),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    stateName: z.literal('pendingRunwayReleasePose'),
    poseFields: z.array(z.enum(['supportId', 'anchorOffsetX', 'anchorOffsetY', 'playerX', 'playerY', 'playerAngle', 'age'])).length(7),
    setterRule: z.literal('capture every early separating runway top-tip contact with player center x<350 and normalY<=-0.9; do not use contact-point x as the suppression basis'),
    lifetimeSeconds: z.number().min(2.1).max(4),
    releaseRule: z.literal('consume once on normal airborne tap by resolving active support transform plus stored offsets, then use standard anchored-release velocity and launch feedback'),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(13),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt11TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(11),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-VIRTUAL-POSE-RECONSTRUCTION'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(13),
  evidenceInputs: z.array(EvidenceRefSchema).min(11),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt12TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(12),
  issueId: z.literal('L1-RUNWAY-RECONSTRUCTION-LINT-ACCEPTANCE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('formal-fixer-acceptance-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    baselineSourceSha256: z.literal('b6955d173525ff9c9aca753fa5f90fd42303c1ac41106614fdeb4fa25442d3c7'),
    retainedTestContractSha256: z.literal('7627f8b8052adb8bd33b6b21f7c0cf935b1d23cba5b4570513ce47bd56984510'),
    acceptanceFailure: z.object({
      path,
      sha256,
      line: z.literal(698),
      rule: z.literal('prefer-const'),
      binding: z.literal('pendingSupport'),
    }).strict(),
    behaviorEvidence: z.object({
      focusedTestsPassed: z.literal(true),
      fullTestsPassed: z.literal(true),
      typecheckPassed: z.literal(true),
      lintPassed: z.literal(false),
      buildRun: z.literal(false),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(4),
    }).strict(),
    rollbackEvidence: z.object({
      path,
      sha256,
      status: z.literal('BLOCKED'),
      testBaselineUnavailable: z.literal(true),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    stateName: z.literal('pendingRunwayReleasePose'),
    correction: z.literal('declare pendingSupport with const because the binding is never reassigned; this removes the lint error without changing runtime behavior'),
    preservedBehavior: z.array(z.string().min(1)).min(3),
    rejectedAlternatives: z.array(z.string().min(1)).min(4),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(1),
    excludedFiles: z.array(path).min(4),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(12),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt12TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(12),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECONSTRUCTION-LINT-ACCEPTANCE'),
  ownedFiles: z.array(path).length(1),
  expectedSourceFileLimit: z.literal(1),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(12),
  evidenceInputs: z.array(EvidenceRefSchema).min(10),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt13TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(13),
  issueId: z.literal('L1-RUNWAY-RECOVERY-PRE-CORRECTION-POSE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    baselineContacts: z.array(z.object({
      ordinal: z.enum(['first', 'second']),
      frame: z.number().int().positive(),
      playerX: z.number(),
      playerY: z.number(),
      playerAngle: z.number(),
      contactPointX: z.number(),
      contactPointY: z.number(),
      normalY: z.number(),
      relativeNormalVelocity: z.number(),
    }).strict()).length(2),
    candidateDivergence: z.object({
      firstPosePlayerY: z.number(),
      firstPoseExpectedY: z.number(),
      firstPoseDeltaY: z.number(),
      secondContactFrame: z.number().int().positive(),
      secondContactExpectedFrame: z.number().int().positive(),
      releaseCount: z.number().int().nonnegative(),
      routePlayerX: z.number(),
      expiryStatus: z.literal('anchored'),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    }).strict(),
    orderingEvidence: z.object({
      responseHelper: z.literal('respondToHardSurface'),
      correctionPixels: z.literal(5),
      correctionDirection: z.literal('contact normal'),
      observedNormalY: z.literal(-1),
      hypothesis: z.string().min(1),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    branch: z.literal('separating runway top sharp contact before hard-surface correction'),
    qualification: z.literal('active collidable level1-runway-main, normalY<=-0.9, relative normal velocity>=0, player center x<350, recovery lockout satisfied'),
    orderingRule: z.literal('capture integrated player pose and support offsets before respondToHardSurface; skip correction and ordinary anchor for this branch'),
    preservedBranches: z.array(z.string().min(1)).min(5),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt13TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(13),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-PRE-CORRECTION-POSE'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14),
  evidenceInputs: z.array(EvidenceRefSchema).min(11),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt14TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(14),
  issueId: z.literal('L1-RUNWAY-RECOVERY-POSE-REARM-GATE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({
    path,
    sha256,
    status: z.literal('BLOCKED'),
    candidateRejected: z.literal(true),
  }).strict(),
  reproduction: z.object({
    baselineContacts: z.array(z.object({
      ordinal: z.enum(['first', 'second']),
      frame: z.number().int().positive(),
      playerX: z.number(),
      playerY: z.number(),
      playerAngle: z.number(),
    }).strict()).length(2),
    candidateOverwrite: z.object({
      firstCaptureFrame: z.literal(216),
      overwriteFrames: z.tuple([z.number().int().positive(), z.number().int().positive()]),
      expectedSecondCaptureFrame: z.literal(302),
      observedSecondCapture: z.literal(false),
      replayEndSeconds: z.number(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    }).strict(),
    lifecycleRequirement: z.object({
      pendingState: z.literal('pendingRunwayReleasePose'),
      setterRule: z.literal('capture only when pending pose is null; do not overwrite a live pose during the same contact manifold'),
      rearmRule: z.literal('after one-shot tap consumption clears the pose, the next qualifying separating contact may capture a new pose'),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1),
    lifecycleStateBranch: z.string().min(1),
    rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']),
    evidence: z.array(path).min(1),
    observation: z.string().min(1),
    inference: z.string().min(1),
    unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    stateName: z.literal('pendingRunwayReleasePose'),
    guard: z.literal('pending pose must be null before setter; a consumed pose clears the guard and permits the next qualifying contact'),
    preservedBranches: z.array(z.string().min(1)).min(4),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({
    includedFiles: z.array(path).length(2),
    excludedFiles: z.array(path).min(3),
    route: z.literal('formal-fixer'),
    expectedSourceFileLimit: z.literal(2),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(13),
  evidenceClassification: z.object({
    observed: z.array(z.string().min(1)).min(1),
    inferred: z.array(z.string().min(1)).min(1),
    unknown: z.array(z.string().min(1)).min(1),
  }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt14TaskPacketSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('fixer-task-packet'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(14),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  role: z.literal('FixerAgent'),
  route: z.literal('formal-fixer'),
  model: z.literal('gpt-6-luna'),
  triagePath: path,
  triageSha256: sha256,
  issueId: z.literal('L1-RUNWAY-RECOVERY-POSE-REARM-GATE'),
  ownedFiles: z.array(path).length(2),
  expectedSourceFileLimit: z.literal(2),
  expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true),
    redCommand: z.string().min(1),
    greenCommand: z.string().min(1),
    regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(13),
  evidenceInputs: z.array(EvidenceRefSchema).min(11),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({
    fixResultPath: path,
    redLogPath: path,
    greenLogPath: path,
    testsLogPath: path,
    lintLogPath: path,
    typecheckLogPath: path,
    buildLogPath: path,
  }).strict(),
  noRuntimeQaClaim: z.literal(true),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt15TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(15),
  issueId: z.literal('L1-RUNWAY-RECOVERY-POSE-FOLLOW-AND-CLEAR-TRACE'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({ path, sha256, status: z.literal('BLOCKED'), candidateRejected: z.literal(true) }).strict(),
  reproduction: z.object({
    validatedPoseReference: z.object({
      path,
      sha256,
      firstTapCorrectionPixels: z.number(),
      secondTapCorrectionPixels: z.number(),
      firstMaxMotionPixels: z.number(),
      secondMaxMotionPixels: z.number(),
      firstMaxRotationRadians: z.number(),
      secondMaxRotationRadians: z.number(),
    }).strict(),
    attempt14Divergence: z.object({
      firstTapCorrectionPixels: z.number(),
      secondPoseFrame: z.literal(302),
      secondPoseClearFrame: z.literal(528),
      scheduledSecondTapFrame: z.literal(553),
      clearLeadFrames: z.literal(25),
      clearReason: z.literal('UNKNOWN_UNINSTRUMENTED'),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    }).strict(),
    traceRequirement: z.object({
      reasonEnum: z.array(z.enum(['tap-consumed', 'ttl-expired', 'reset', 'load-level', 'terminal', 'inactive-support', 'non-runway-contact', 'unknown'])).min(8),
      fields: z.array(z.string().min(1)).min(10),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1), lifecycleStateBranch: z.string().min(1), rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']), evidence: z.array(path).min(1),
    observation: z.string().min(1), inference: z.string().min(1), unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    stateName: z.literal('pendingRunwayReleasePose'),
    followRule: z.literal('while pending, preserve normal airborne motion but apply a bounded follow toward the current support-relative pose so the visible player remains within the tap correction budget'),
    diagnosticRule: z.literal('every clear records an explicit reason, frame, elapsed, age, status, anchorId, support activity, and player pose'),
    preservedBranches: z.array(z.string().min(1)).min(5),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({ includedFiles: z.array(path).length(2), excludedFiles: z.array(path).min(3), route: z.literal('formal-fixer'), expectedSourceFileLimit: z.literal(2) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14),
  evidenceClassification: z.object({ observed: z.array(z.string().min(1)).min(1), inferred: z.array(z.string().min(1)).min(1), unknown: z.array(z.string().min(1)).min(1) }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt15TaskPacketSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('fixer-task-packet'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(15),
  targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE), role: z.literal('FixerAgent'), route: z.literal('formal-fixer'), model: z.literal('gpt-6-luna'),
  triagePath: path, triageSha256: sha256, issueId: z.literal('L1-RUNWAY-RECOVERY-POSE-FOLLOW-AND-CLEAR-TRACE'),
  ownedFiles: z.array(path).length(2), expectedSourceFileLimit: z.literal(2), expectedHypothesis: z.string().min(1),
  testFirst: z.object({ redRequired: z.literal(true), redCommand: z.string().min(1), greenCommand: z.string().min(1), regressionCommand: z.string().min(1) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14), evidenceInputs: z.array(EvidenceRefSchema).min(11), contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({ fixResultPath: path, redLogPath: path, greenLogPath: path, testsLogPath: path, lintLogPath: path, typecheckLogPath: path, buildLogPath: path }).strict(),
  noRuntimeQaClaim: z.literal(true), createdAt: z.string().datetime(),
}).strict();

export const Attempt16TriageSchema = z.object({
  schemaVersion: z.literal(1),
  artifactType: z.literal('repair-triage'),
  cycleId: z.literal(CYCLE_ID),
  attempt: z.literal(16),
  issueId: z.literal('L1-RUNWAY-RECOVERY-LIFECYCLE-REARM'),
  targetGame: z.literal(TARGET_GAME),
  workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'),
  status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({ path, sha256, status: z.literal('BLOCKED'), candidateRejected: z.literal(true) }).strict(),
  reproduction: z.object({
    attempt15Divergence: z.object({
      expectedCaptureFrames: z.tuple([z.literal(216), z.literal(302)]),
      observedCaptureFrames: z.tuple([z.literal(216), z.literal(296)]),
      secondPose: z.object({ playerX: z.number(), playerY: z.number(), playerAngle: z.number() }).strict(),
      replay: z.object({ elapsedSeconds: z.number(), status: z.literal('failed'), playerX: z.number() }).strict(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    }).strict(),
    lifecycleSignals: z.object({
      postConsumeTap: z.object({ waitSeconds: z.literal(0.14), expectedEvent: z.literal('flip'), observedEvent: z.literal('launch') }).strict(),
      ttlStep: z.object({ preAgeSeconds: z.number(), expectedPending: z.literal(false), observedPendingAgeSeconds: z.number() }).strict(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(2),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1), lifecycleStateBranch: z.string().min(1), rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']), evidence: z.array(path).min(1),
    observation: z.string().min(1), inference: z.string().min(1), unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    stateName: z.literal('pendingRunwayReleasePose'),
    rearmRule: z.literal('after tap consumption or TTL expiry, disarm the setter until a fresh qualifying runway contact edge is observed; do not rearm from the same contact manifold'),
    diagnosticRule: z.literal('record ordered contact-edge, set, consume, expire, clear, and rearm events with frame, elapsed, age, status, anchorId, pose, and collision context'),
    preservedBranches: z.array(z.string().min(1)).min(5),
    rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({ includedFiles: z.array(path).length(2), excludedFiles: z.array(path).min(3), route: z.literal('formal-fixer'), expectedSourceFileLimit: z.literal(2) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14),
  evidenceClassification: z.object({ observed: z.array(z.string().min(1)).min(1), inferred: z.array(z.string().min(1)).min(1), unknown: z.array(z.string().min(1)).min(1) }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt16TaskPacketSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('fixer-task-packet'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(16),
  targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE), role: z.literal('FixerAgent'), route: z.literal('formal-fixer'), model: z.literal('gpt-6-luna'),
  triagePath: path, triageSha256: sha256, issueId: z.literal('L1-RUNWAY-RECOVERY-LIFECYCLE-REARM'),
  ownedFiles: z.array(path).length(2), expectedSourceFileLimit: z.literal(2), expectedHypothesis: z.string().min(1),
  testFirst: z.object({ redRequired: z.literal(true), redCommand: z.string().min(1), greenCommand: z.string().min(1), regressionCommand: z.string().min(1) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14), evidenceInputs: z.array(EvidenceRefSchema).min(12), contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({ fixResultPath: path, redLogPath: path, greenLogPath: path, testsLogPath: path, lintLogPath: path, typecheckLogPath: path, buildLogPath: path }).strict(),
  noRuntimeQaClaim: z.literal(true), createdAt: z.string().datetime(),
}).strict();

export const Attempt17TriageSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('repair-triage'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(17),
  issueId: z.literal('L1-RUNWAY-RECOVERY-PER-EDGE-LATCH'), targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'), status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({ path, sha256, status: z.literal('BLOCKED'), candidateRejected: z.literal(true) }).strict(),
  reproduction: z.object({
    attempt16Divergence: z.object({
      expectedCaptureFrames: z.tuple([z.literal(216), z.literal(302)]), observedCaptureFrames: z.array(z.number().int().positive()).min(1),
      firstTapPositionCorrectionPixels: z.number(), firstTapAngleCorrectionRadians: z.number(),
      postLaunchPlayerX: z.number(), selectorObserved: z.literal('undefined'), replayStatus: z.literal('failed'), replayElapsedSeconds: z.number(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    }).strict(),
    edgeRequirement: z.object({
      key: z.literal('supportId + sharp contact part + top-face class'),
      duplicateRule: z.literal('do not overwrite or re-capture while the keyed contact manifold remains active'),
      rearmRule: z.literal('rearm only after the keyed contact leaves and a distinct qualifying edge is observed'),
      separatingTopRule: z.literal('a qualifying separating runway top contact remains airborne and never falls through to ordinary anchor'),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1), lifecycleStateBranch: z.string().min(1), rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']), evidence: z.array(path).min(1),
    observation: z.string().min(1), inference: z.string().min(1), unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1), changeHypothesis: z.string().min(1), stateName: z.literal('pendingRunwayReleasePose'),
    edgeRule: z.literal('latch the runway support plus sharp top-contact class per edge; duplicate edges cannot overwrite, and only a clear keyed manifold can rearm'),
    selectorRule: z.literal('runtime release selector returns the same reachable authored support selected by the recorded candidate trace, including white-branch and late lift-plank fixtures'),
    preservedBranches: z.array(z.string().min(1)).min(5), rejectedAlternatives: z.array(z.string().min(1)).min(5),
  }).strict(),
  scope: z.object({ includedFiles: z.array(path).length(2), excludedFiles: z.array(path).min(3), route: z.literal('formal-fixer'), expectedSourceFileLimit: z.literal(2) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14),
  evidenceClassification: z.object({ observed: z.array(z.string().min(1)).min(1), inferred: z.array(z.string().min(1)).min(1), unknown: z.array(z.string().min(1)).min(1) }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt17TaskPacketSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('fixer-task-packet'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(17),
  targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE), role: z.literal('FixerAgent'), route: z.literal('formal-fixer'), model: z.literal('gpt-6-luna'),
  triagePath: path, triageSha256: sha256, issueId: z.literal('L1-RUNWAY-RECOVERY-PER-EDGE-LATCH'), ownedFiles: z.array(path).length(2), expectedSourceFileLimit: z.literal(2), expectedHypothesis: z.string().min(1),
  testFirst: z.object({ redRequired: z.literal(true), redCommand: z.string().min(1), greenCommand: z.string().min(1), regressionCommand: z.string().min(1) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(14), evidenceInputs: z.array(EvidenceRefSchema).min(12), contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({ fixResultPath: path, redLogPath: path, greenLogPath: path, testsLogPath: path, lintLogPath: path, typecheckLogPath: path, buildLogPath: path }).strict(),
  noRuntimeQaClaim: z.literal(true), createdAt: z.string().datetime(),
}).strict();

export const Attempt18TriageSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('repair-triage'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(18),
  issueId: z.literal('L1-RUNWAY-RECOVERY-CAPTURE-STAGE'), targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'), status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({ path, sha256, status: z.literal('BLOCKED'), candidateRejected: z.literal(true) }).strict(),
  reproduction: z.object({
    attempt17Divergence: z.object({
      expectedCaptureFrames: z.tuple([z.literal(216), z.literal(302)]), observedCaptureFrames: z.array(z.number().int().positive()).min(1),
      expectedFirstPose: z.object({ playerX: z.number(), playerY: z.number(), playerAngle: z.number() }).strict(),
      observedFirstRelease: z.object({ playerX: z.number(), playerY: z.number(), playerAngle: z.number() }).strict(),
      firstTapPositionCorrectionPixels: z.number(), selectorStatus: z.literal('passed'), replayStatus: z.literal('failed'), replayElapsedSeconds: z.number(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    }).strict(),
    captureContract: z.object({
      requiredStage: z.literal('post-integration pre-response/pre-anchor player transform'),
      forbiddenStages: z.array(z.enum(['sweep-impact interpolation', 'post-separation correction', 'post-anchor transform', 'contact-point coordinates'])).min(3),
      retainedFields: z.array(z.enum(['supportId', 'anchorOffsetX', 'anchorOffsetY', 'playerX', 'playerY', 'playerAngle', 'age'])).length(7),
      coordinateInvariant: z.literal('support.x + anchorOffsetX == playerX and support.y + anchorOffsetY == playerY at capture'),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1), lifecycleStateBranch: z.string().min(1), rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']), evidence: z.array(path).min(1),
    observation: z.string().min(1), inference: z.string().min(1), unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    stateName: z.literal('pendingRunwayReleasePose'),
    captureRule: z.literal('on a qualifying early separating runway top sharp contact, snapshot the integrated player transform before any separation response or ordinary anchor and retain both absolute pose fields and support-relative offsets'),
    releaseRule: z.literal('consume the immutable support-relative pose once, resolve the active support transform, restore the stored player angle, and emit standard anchored-release launch semantics'),
    falsifier: z.literal('if the pre-anchor snapshot at frame216 equals the validated pose and exact release still misses frame302, capture-stage semantics are rejected in favor of collision/latch diagnosis'),
    preservedBranches: z.array(z.string().min(1)).min(6), rejectedAlternatives: z.array(z.string().min(1)).min(6),
  }).strict(),
  scope: z.object({ includedFiles: z.array(path).length(2), excludedFiles: z.array(path).min(3), route: z.literal('formal-fixer'), expectedSourceFileLimit: z.literal(2) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(15),
  evidenceClassification: z.object({ observed: z.array(z.string().min(1)).min(1), inferred: z.array(z.string().min(1)).min(1), unknown: z.array(z.string().min(1)).min(1) }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt18TaskPacketSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('fixer-task-packet'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(18),
  targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE), role: z.literal('FixerAgent'), route: z.literal('formal-fixer'), model: z.literal('gpt-6-luna'),
  triagePath: path, triageSha256: sha256, issueId: z.literal('L1-RUNWAY-RECOVERY-CAPTURE-STAGE'), ownedFiles: z.array(path).length(2), expectedSourceFileLimit: z.literal(2), expectedHypothesis: z.string().min(1),
  testFirst: z.object({ redRequired: z.literal(true), redCommand: z.string().min(1), greenCommand: z.string().min(1), regressionCommand: z.string().min(1) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(15), evidenceInputs: z.array(EvidenceRefSchema).min(12), contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({ fixResultPath: path, redLogPath: path, greenLogPath: path, testsLogPath: path, lintLogPath: path, typecheckLogPath: path, buildLogPath: path }).strict(),
  noRuntimeQaClaim: z.literal(true), createdAt: z.string().datetime(),
}).strict();

export const Attempt19TriageSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('repair-triage'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(19),
  issueId: z.literal('L1-RUNWAY-RECOVERY-KINEMATIC-ENVELOPE'), targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'), status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({ path, sha256, status: z.literal('BLOCKED'), candidateRejected: z.literal(true) }).strict(),
  reproduction: z.object({
    attempt18Divergence: z.object({
      expectedCaptureFrames: z.tuple([z.literal(216), z.literal(302)]), observedCaptureFrames: z.tuple([z.literal(216), z.literal(302)]),
      capturePoses: z.array(z.object({ playerX: z.number(), playerY: z.number(), playerAngle: z.number() }).strict()).length(2),
      pendingPositionDriftPixels: z.tuple([z.number().positive(), z.number().positive()]),
      pendingAngleDriftRadians: z.tuple([z.number().positive(), z.number().positive()]),
      tapPositionCorrectionsPixels: z.tuple([z.number().positive(), z.number().positive()]),
      tapAngleCorrectionsRadians: z.tuple([z.number().positive(), z.number().positive()]),
      focusedResult: z.object({ passed: z.literal(19), failed: z.literal(1), replayElapsedSeconds: z.number(), replayStatus: z.literal('airborne') }).strict(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(3),
    }).strict(),
    driftContract: z.object({
      targetPositionEnvelopePixels: z.literal(8), targetAngleEnvelopeRadians: z.literal(0.4),
      appliesWhen: z.literal('airborne with pendingRunwayReleasePose after capture and before one-time tap consumption'),
      mechanism: z.literal('post-integration projection into a pose-relative displacement envelope; no follow gain or support recapture'),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1), lifecycleStateBranch: z.string().min(1), rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']), evidence: z.array(path).min(1),
    observation: z.string().min(1), inference: z.string().min(1), unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1),
    changeHypothesis: z.string().min(1),
    stateName: z.literal('pendingRunwayReleasePose'),
    envelopeRule: z.literal('after each fixed-step integration while pending, project player center to <=8px and angle to <=0.4rad from the immutable captured pose; preserve normal airborne state and do not recapture or follow the support'),
    stageRule: z.literal('apply only after the capture step and before contact/failure evaluation; leave capture coordinates and support-relative reconstruction unchanged'),
    falsifier: z.literal('if exact captures change, either tap exceeds <=10px or <=0.5rad, replay misses 11.9s/x>2000, or preserved lifecycle/contact selectors regress, reject the kinematic envelope'),
    preservedBranches: z.array(z.string().min(1)).min(7), rejectedAlternatives: z.array(z.string().min(1)).min(7),
  }).strict(),
  scope: z.object({ includedFiles: z.array(path).length(2), excludedFiles: z.array(path).min(3), route: z.literal('formal-fixer'), expectedSourceFileLimit: z.literal(2) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(16),
  evidenceClassification: z.object({ observed: z.array(z.string().min(1)).min(1), inferred: z.array(z.string().min(1)).min(1), unknown: z.array(z.string().min(1)).min(1) }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt19TaskPacketSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('fixer-task-packet'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(19),
  targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE), role: z.literal('FixerAgent'), route: z.literal('formal-fixer'), model: z.literal('gpt-6-luna'),
  triagePath: path, triageSha256: sha256, issueId: z.literal('L1-RUNWAY-RECOVERY-KINEMATIC-ENVELOPE'), ownedFiles: z.array(path).length(2), expectedSourceFileLimit: z.literal(2), expectedHypothesis: z.string().min(1),
  testFirst: z.object({ redRequired: z.literal(true), redCommand: z.string().min(1), greenCommand: z.string().min(1), regressionCommand: z.string().min(1) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(16), evidenceInputs: z.array(EvidenceRefSchema).min(13), contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({ fixResultPath: path, redLogPath: path, greenLogPath: path, testsLogPath: path, lintLogPath: path, typecheckLogPath: path, buildLogPath: path }).strict(),
  noRuntimeQaClaim: z.literal(true), createdAt: z.string().datetime(),
}).strict();

export const Attempt20TriageSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('repair-triage'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(20),
  issueId: z.literal('L1-RUNWAY-RECOVERY-AGE-SELECTOR-CONTRACT'), targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'), status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({ path, sha256, status: z.literal('BLOCKED'), candidateRejected: z.literal(true) }).strict(),
  reproduction: z.object({
    attempt19Divergence: z.object({
      focusedTests: z.object({ total: z.literal(21), passed: z.literal(19), failed: z.literal(2) }).strict(),
      captures: z.tuple([z.literal(216), z.literal(302)]),
      capturePoses: z.array(z.object({ playerX: z.number(), playerY: z.number(), playerAngle: z.number() }).strict()).length(2),
      envelope: z.object({ maxCenterMotionPixels: z.tuple([z.number(), z.number()]), maxOrientationMotionRadians: z.tuple([z.number(), z.number()]) }).strict(),
      replay: z.object({ elapsedSeconds: z.number(), playerX: z.number(), status: z.literal('airborne') }).strict(),
      ageBoundary: z.object({ observedSeconds: z.literal(2.091666666666662), requiredMinimumSeconds: z.literal(2.092), deficitSeconds: z.number().positive(), cause: z.literal('one fixed-step initialization offset') }).strict(),
      selectorBoundary: z.object({ earlyExpected: z.literal('white-branch'), lateExpected: z.literal('lift-plank'), earlyObserved: z.literal('undefined'), lateObserved: z.literal('undefined') }).strict(),
      evidence: z.array(z.object({ path, sha256 }).strict()).min(4),
    }).strict(),
    contract: z.object({
      ageRule: z.literal('capture age starts at one fixed simulation step, then advances once per fixed step; TTL remains 2.35 seconds'),
      selectorRule: z.literal('runtime selectRunwayReleaseSupport reuses the deterministic authored-candidate reachability ordering used by the test trace'),
      preservedMechanics: z.literal('do not change capture frames, stored pose fields, post-capture envelope, tap release, or ordinary no-pending flight'),
    }).strict(),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1), lifecycleStateBranch: z.string().min(1), rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']), evidence: z.array(path).min(1),
    observation: z.string().min(1), inference: z.string().min(1), unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1), changeHypothesis: z.string().min(1), stateName: z.literal('pendingRunwayReleasePose'),
    ageRule: z.literal('initialize captured pose age to FIXED_STEP so the capture step is represented, then increment exactly once per fixed update; do not relax the 2.35s TTL'),
    selectorRule: z.literal('add a read-only runtime selector that evaluates active collidable authored supports ahead of the player using the same reachable vertical corridor ordering as traceReleaseCandidates'),
    falsifier: z.literal('if age remains below 2.092 after the initialization correction, selector IDs disagree with early white-branch or late lift-plank traces, or any Attempt19 capture/envelope/replay assertion regresses, reject this contract hypothesis'),
    preservedBranches: z.array(z.string().min(1)).min(8), rejectedAlternatives: z.array(z.string().min(1)).min(8),
  }).strict(),
  scope: z.object({ includedFiles: z.array(path).length(2), excludedFiles: z.array(path).min(3), route: z.literal('formal-fixer'), expectedSourceFileLimit: z.literal(2) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(16),
  evidenceClassification: z.object({ observed: z.array(z.string().min(1)).min(1), inferred: z.array(z.string().min(1)).min(1), unknown: z.array(z.string().min(1)).min(1) }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt20TaskPacketSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('fixer-task-packet'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(20),
  targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE), role: z.literal('FixerAgent'), route: z.literal('formal-fixer'), model: z.literal('gpt-6-luna'),
  triagePath: path, triageSha256: sha256, issueId: z.literal('L1-RUNWAY-RECOVERY-AGE-SELECTOR-CONTRACT'), ownedFiles: z.array(path).length(2), expectedSourceFileLimit: z.literal(2), expectedHypothesis: z.string().min(1),
  testFirst: z.object({ redRequired: z.literal(true), redCommand: z.string().min(1), greenCommand: z.string().min(1), regressionCommand: z.string().min(1) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(16), evidenceInputs: z.array(EvidenceRefSchema).min(13), contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({ fixResultPath: path, redLogPath: path, greenLogPath: path, testsLogPath: path, lintLogPath: path, typecheckLogPath: path, buildLogPath: path }).strict(),
  noRuntimeQaClaim: z.literal(true), createdAt: z.string().datetime(),
}).strict();

const Attempt21EvidenceSchema = z.object({ path, sha256, role: EvidenceRefSchema.shape.role }).strict();

export const Attempt21TriageSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('repair-triage'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(21),
  issueId: z.literal('L1-RUNWAY-RECOVERY-ATTEMPT19-PREREQUISITE-RECONSTRUCTION'), targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE),
  classification: z.literal('natural-level1-runway-recovery-blocker'), status: z.literal('VALIDATED_REPRODUCTION'),
  priorAttempt: z.object({ path, sha256, status: z.literal('BLOCKED'), candidateRejected: z.literal(true) }).strict(),
  reproduction: z.object({
    attempt20Divergence: z.object({
      focusedTests: z.object({ total: z.literal(20), passed: z.literal(16), failed: z.literal(4) }).strict(),
      prerequisite: z.object({
        sourceMatchesBaseline: z.literal(true),
        sourceSha256: z.literal('b6955d173525ff9c9aca753fa5f90fd42303c1ac41106614fdeb4fa25442d3c7'),
        testSha256: z.literal('7627f8b8052adb8bd33b6b21f7c0cf935b1d23cba5b4570513ce47bd56984510'),
        replayTerminalSeconds: z.literal(6.5),
        captures: z.array(z.number().int()).length(0),
        cause: z.literal('Attempt19 pending-pose capture/envelope/release prerequisite was not reconstructed after baseline restore'),
      }).strict(),
      attempt19NearPass: z.object({
        focusedTests: z.object({ total: z.literal(21), passed: z.literal(19), failed: z.literal(2) }).strict(),
        captures: z.tuple([z.literal(216), z.literal(302)]),
        envelope: z.object({ centerPixels: z.tuple([z.literal(8), z.literal(8)]), angleRadians: z.tuple([z.literal(0.4), z.literal(0.4)]) }).strict(),
        replay: z.object({ elapsedSeconds: z.literal(14.6167), playerX: z.literal(2132.7), status: z.literal('airborne') }).strict(),
        remainingFailures: z.tuple([z.literal('second capture release age 2.091666666666662 < 2.092'), z.literal('runtime selector returned undefined for white-branch and lift-plank fixtures')]),
      }).strict(),
      evidence: z.array(Attempt21EvidenceSchema).min(6),
    }).strict(),
    scopeOfHypothesis: z.string().min(1),
  }).strict(),
  reproductionMatrix: z.array(z.object({
    objectType: z.string().min(1), lifecycleStateBranch: z.string().min(1), rendererPresentationPath: z.string().min(1),
    viewport: z.enum(['390x844', '1100x720', 'deterministic-fixed-step']), evidence: z.array(Attempt21EvidenceSchema).min(1),
    observed: z.string().min(1), inferred: z.string().min(1), unknowns: z.array(z.string().min(1)).min(1),
  }).strict()).min(4),
  rootCauseHypothesis: z.object({
    evidence: z.string().min(1), changeHypothesis: z.string().min(1), stateName: z.literal('pendingRunwayReleasePose'),
    ageRule: z.literal('initialize capture age to exactly FIXED_STEP, increment once per fixed update, keep TTL at 2.35 seconds'),
    selectorRule: z.literal('implement a read-only runtime selector with the same active, collidable, ahead, reachable-corridor and earliest-entry ordering as the independent diagnostic trace'),
    falsifier: z.literal('reject if Attempt19 capture/envelope/release/replay assertions regress, second release age remains below 2.092, selector fixtures do not return white-branch and lift-plank, or TTL/ordinary branches change'),
    preservedBranches: z.array(z.string().min(1)).min(8), rejectedAlternatives: z.array(z.string().min(1)).min(6),
  }).strict(),
  scope: z.object({ includedFiles: z.array(path).length(2), excludedFiles: z.array(path).min(3), route: z.literal('formal-fixer'), expectedSourceFileLimit: z.literal(2) }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(16),
  evidenceClassification: z.object({ observed: z.array(z.string().min(1)).min(1), inferred: z.array(z.string().min(1)).min(1), unknown: z.array(z.string().min(1)).min(1) }).strict(),
  createdAt: z.string().datetime(),
}).strict();

export const Attempt21TaskPacketSchema = z.object({
  schemaVersion: z.literal(1), artifactType: z.literal('fixer-task-packet'), cycleId: z.literal(CYCLE_ID), attempt: z.literal(21),
  targetGame: z.literal(TARGET_GAME), workspace: z.literal(WORKSPACE), role: z.literal('FixerAgent'), route: z.literal('formal-fixer'), model: z.literal('gpt-6-luna'),
  triagePath: path, triageSha256: sha256, issueId: z.literal('L1-RUNWAY-RECOVERY-ATTEMPT19-PREREQUISITE-RECONSTRUCTION'),
  ownedFiles: z.array(path).length(2), expectedSourceFileLimit: z.literal(2), expectedHypothesis: z.string().min(1),
  testFirst: z.object({
    redRequired: z.literal(true), redCommand: z.string().min(1), redMustFailBeforeEdits: z.literal(true),
    greenCommand: z.string().min(1), greenMustPassAfterMinimalEdits: z.literal(true), regressionCommand: z.string().min(1),
  }).strict(),
  acceptanceCriteria: z.array(z.string().min(1)).min(16), evidenceInputs: z.array(Attempt21EvidenceSchema).min(8),
  contextIntentionallyOmitted: z.array(z.string().min(1)).min(3),
  requiredOutputs: z.object({ fixResultPath: path, redLogPath: path, greenLogPath: path, testsLogPath: path, lintLogPath: path, typecheckLogPath: path, buildLogPath: path }).strict(),
  independentQaRequired: z.literal(true), noRuntimeQaClaim: z.literal(true), createdAt: z.string().datetime(),
}).strict();
