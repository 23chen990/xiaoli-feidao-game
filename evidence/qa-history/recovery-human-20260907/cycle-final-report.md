# 小李飞刀首关：修复周期最终报告

周期：`human-review-20260907-l1-recovery`  
目标游戏：`mobile-slice-adaptation-20260830`  
唯一工作区：`runs/mobile-slice-adaptation-20260830/workspace/prototype-a`

## 结果

本次明确授权的 5 次修复额度已用完，周期状态为 `BLOCKED_MAX_ATTEMPTS`。最终构建为 `dist/index.html`，SHA-256 为 `10cba7632922f9fdeaa2eb33f7a05aa09bd3dbb7ca45a2db24acac282c9fd9a6`。最终工程测试 192/192、lint、typecheck 均通过；这些结果不能替代玩家视角验收。

最终 12 项矩阵有 8 项 PASS、4 项 BLOCKED：

| 维度 | 手机 390×844 | 桌面 1100×720 | 说明 |
|---|---|---|---|
| 首个目标可见 | PASS | PASS | 第 3 次出现的遮挡回归已在第 4 次恢复；刀、目标和承托同屏可辨 |
| 开场接地 | PASS | PASS | 开场基座有厚度并接到可见地面 |
| 奖励顺序与附着 | PASS | PASS | 切面/分离后局部奖励出现并同步累计；未强加无来源的帧延迟 |
| 危险与失败恢复 | PASS | PASS | 尖刺提前可见，失败原因和重试入口可见 |
| 堆叠承托→切开→掉落→落地 | BLOCKED | BLOCKED | 工程测试证明同一对象关系和事件；自然录屏没有覆盖完整可见链 |
| 终点结算与重玩 | BLOCKED | BLOCKED | 桌面在第 4 次曾自然通关，但最后一次复核两视口都在前段失败；按当前构建最终证据仍不完整 |

## 已修复的根因

- 完整球体使用没有几何分组的材质数组，导致完整态不提交可见三角形；改为与几何一致的材质路径。
- 开场台面没有进入落地基座分支；补上开场对象的可见接地和厚度。
- 梯形/彩虹堆叠原先有同形装饰不参与碰撞；改为可碰撞、真实承托、接触切开并释放上层对象。
- 支撑物在玩家空中时被错误冻结；恢复支撑丢失后的重力更新，并校正部分后段台面的承托。
- 终点标签容器重复施加坐标偏移；改为投影坐标与视口布局一致，并增加安全区域检查。
- 首关旧测试依赖过期坐标和密度假设；用当前契约要求的关系、事件和值域替代，未跳过或放宽测试。

## 仍需人工复核的具体内容

1. 用正常输入录制同一 `stack-a` 或 `stack-b`：完整承托画面、刀接触、同一目标切面、上层失去承托、下落、落地，需在手机和桌面各有连续证据。
2. 在同一构建上用正常输入取得手机终点选择、世界结果、结算和重玩；桌面已有一次通过记录，但不能代替手机。
3. 若人工复核确认上述两项均正常，只需补交验证，不应再次修改玩法；若发现同一对象没有掉落或手机终点确实无法到达，再开启新的、明确授权的人工修复周期。

所有历史修复、QA、录屏、构建哈希和失败原因均保存在本目录 `attempt-1` 至 `attempt-5` 中，没有覆盖旧记录。
