import {readFileSync,writeFileSync} from 'node:fs';import {resolve} from 'node:path';import {QaReportSchema} from '../../../src/schemas/index.ts';import {TaskSchema} from './recovery-schemas.ts';
const run=resolve('runs/mobile-slice-adaptation-20260830');const out=`${run}/qa-recovery-20260907`;const ws=`${run}/workspace/prototype-a`;
const report=QaReportSchema.parse({schemaVersion:1,passed:false,checks:[{name:'exact opening support branch inspected after reproduction matrix',passed:true,evidence:`${out}/reproduction-matrix.json L1-GROUND; ${ws}/src/three-world-renderer.ts:324`},{name:'opening-table-a and opening-table-b grounded renderer membership',passed:false,evidence:`${ws}/src/game-levels.ts:179 defines opening-table-a and opening-table-b; ${ws}/src/three-world-renderer.ts:324 isGroundedTabletop excludes both. Their thin support meshes still render at :283. ${out}/independent-qa/desktop-1-startup.png shows suspended opening white slabs.`},{name:'interactive stack does not silently bypass collision',passed:false,evidence:`${ws}/src/game-levels.ts:178 sets level1-ladder-stack-a/b collidable:false while pathRole is branch; ${ws}/src/game-core.ts:932 skips these blocks in collision. ${out}/active-slice-contract.json L1-STACK requires actual interaction.`}],issues:[{id:'L1-OPENING-TABLE-BRANCH',severity:'error',message:'开场 opening-table-a/b 没进入落地基座渲染分支；此前只修了其他台面，无法关闭开场悬浮投诉。',evidence:`${out}/reproduction-matrix.json L1-GROUND-390/L1-GROUND-1100; ${out}/independent-qa/desktop-1-startup.png; ${ws}/src/three-world-renderer.ts:324`},{id:'L1-STACK-DECORATIVE-SUBSTITUTION',severity:'error',message:'两组命名为 branch 的梯形堆叠仍 collidable:false，碰撞路径跳过。只能证明装饰存在，不能满足刀接触后散开/失去支撑下落的要求；自然输入精确分支证据仍待 QA。',evidence:`${ws}/src/game-levels.ts:178; ${ws}/src/game-core.ts:932; ${out}/active-slice-contract.json L1-STACK`}],screenshots:[`${out}/independent-qa/desktop-1-startup.png`,`${out}/independent-qa/portrait-1-startup.png`],consoleLog:`${out}/independent-qa/desktop-1-console.json`,testedAt:new Date().toISOString()});
writeFileSync(`${out}/source-diagnosis.json`,JSON.stringify(report,null,2)+'\n');
const old=TaskSchema.parse(JSON.parse(readFileSync(`${out}/qa-task-reference-update.json`,'utf8')));
const next=TaskSchema.parse({...old,instructions:[...old.instructions,`Parent read-only exact-branch source diagnosis is QaReportSchema validated at ${out}/source-diagnosis.json. It identifies opening-table-a/b missing from isGroundedTabletop allowlist and ladder-stack-a/b collision bypass. Treat source facts as engineering evidence only; retain your independent pixel/trace verdict, and do not count source diagnosis as natural branch proof. Finish bounded QA and return validated result.`]});
writeFileSync(`${out}/qa-task-diagnosis-update.json`,JSON.stringify(next,null,2)+'\n');console.log(JSON.stringify({validated:true,issueIds:report.issues.map(i=>i.id),taskPath:`${out}/qa-task-diagnosis-update.json`}));
